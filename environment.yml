name: climpred-dev
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.9
  - climpred
  - esmpy=*=mpi*  # Ensures MPI works with version of esmpy.
  - xesmf
  - xskillscore>=0.0.20
  - xrft
  - xclim>=0.30.0
  - bias_correction
  # Visualization
  - matplotlib-base
  - nc-time-axis>=1.4.0 
